# ── Telegram bot (bot.py) ──────────────────────────────
# Единственный компонент, который читает переменные окружения.
# На Railway задаются в Variables.

TOKEN=your-telegram-bot-token
WEBAPP_URL=https://ewgeen239-netizen.github.io/test/

# Supabase — рабочий проект mkuwkntdcpfsxhqblkic
SUPABASE_URL=https://mkuwkntdcpfsxhqblkic.supabase.co
# anon (publishable) — публичный ключ, только чтение рейтинга
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1rdXdrbnRkY3Bmc3hocWJsa2ljIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI5NTc3MDQsImV4cCI6MjA5ODUzMzcwNH0.k0_tWR0SgvWKBWmfkb9Z7qRLhHSAvkmmpGnQWEFm2f8

# service_role — СЕКРЕТ, в репозиторий не коммитить.
# Нужен боту для таблицы bot_users (запись/чтение в обход RLS).
# Без него /broadcast и /stats отключены. Settings → API → service_role.
SUPABASE_SERVICE_KEY=

# Telegram ID админов через запятую — кому доступны /broadcast и /stats
ADMIN_TELEGRAM_IDS=6166155438


# ── Mini App (index.html) ─────────────────────────────
# Переменных окружения НЕ читает: это статичная страница на GitHub Pages,
# адрес и anon-ключ зашиты прямо в неё константами SB_URL и SB_KEY.
# Меняешь проект — правишь их в index.html (и project_id в supabase/config.toml).


# ── Edge Function submit-rank ─────────────────────────
# Задаются НЕ здесь, а секретами функции:
#   supabase secrets set BOT_TOKEN=... PROJECT_URL=... SERVICE_ROLE_KEY=...
# BOT_TOKEN        — токен бота, для проверки подписи Telegram initData
# PROJECT_URL      — https://mkuwkntdcpfsxhqblkic.supabase.co
# SERVICE_ROLE_KEY — service_role, СЕКРЕТ
